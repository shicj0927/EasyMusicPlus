{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build UI",
      "type": "shell",
      "command": "find ui -name '*.ui' | while read f; do pyuic6 \"$f\" -o \"$(dirname \"$f\")/ui_$(basename \"${f%.ui}\").py\"; done"
    },
    {
      "label": "Run PyQt",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/python",
      "args": [
        "main.py"
      ],
      "dependsOn": [
        "Build UI",
        "make project view"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "make project view",
      "type": "shell",
      "command": "python project_view.py > project_view.txt"
    }
  ]
}